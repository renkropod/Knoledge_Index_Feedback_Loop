---
title: Gemini 3.1 Pro
source: hacker_news
url: https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-pro/
points: 963
---

Title: Gemini 3.1 Pro
Points: 963, Author: MallocVoidstar, Comments: 914

One CLI for all of Google Workspace — built for humans and AI agents.
Drive, Gmail, Calendar, and every Workspace API. Zero boilerplate. Structured JSON output. 40+ agent skills included.
an officially supported Google product.
npm install -g @googleworkspace/cli
doesn't ship a static list of commands. It reads Google's own
at runtime and builds its entire command surface dynamically. When Google Workspace adds an API endpoint or method,
This project is under active development. Expect breaking changes as we march toward v1.0.
(or download a pre-built binary from
— required for OAuth credentials. You can create one via the
with access to Google Workspace
npm install -g @googleworkspace/cli
The npm package bundles pre-built native binaries for your OS and architecture.
Pre-built binaries are also available on the
cargo install --git https://github.com/googleworkspace/cli --locked
A Nix flake is also available at
nix run github:googleworkspace/cli
On macOS and Linux, you can also install via
brew install googleworkspace-cli
walks you through Google Cloud project config
to preview requests, and auto‑pagination.
— every response is structured JSON. Pair it with the included agent skills and your LLM can manage Workspace without custom tooling.
gws sheets spreadsheets create --json
{"properties": {"title": "Q1 Budget"}}
gws chat spaces messages create \
Introspect any method's request/response schema
Stream paginated results as NDJSON
The CLI supports multiple auth workflows so it works on your laptop, in CI, and on a server.
GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE
Credentials are encrypted at rest (AES-256-GCM) with the key stored in your OS keyring (or
GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=file
one-time: creates a Cloud project, enables APIs, logs you in
subsequent scope selection and login
If your OAuth app is unverified (testing mode),
Google limits consent to ~25 scopes. The
for unverified apps (especially for
Choose individual services instead to filter the scope picker:
gws auth login -s drive,gmail,sheets
Manual OAuth setup (Google Cloud Console)
cannot automate project/client creation, or when you want explicit control.
Open Google Cloud Console in the target project:
https://console.cloud.google.com/apis/credentials/consent?project=<PROJECT_ID>
https://console.cloud.google.com/apis/credentials?project=<PROJECT_ID>
Configure OAuth branding/audience if prompted:
Download the client JSON and save it to:
~/.config/gws/client_secret.json
You must add yourself as a test user.
In the OAuth consent screen, click
and enter your Google account email. Without this,
login will fail with a generic "Access blocked" error.
Browser-assisted auth (human or agent)
You can complete OAuth either manually or with browser automation.
, open the printed URL, approve scopes.
: the agent opens the URL, selects account, handles consent prompts, and returns control once the localhost callback succeeds.
"Google hasn't verified this app"
If scope checkboxes appear, select 
