---
title: Wikipedia deprecates Archive.today, starts removing archive links
source: hacker_news
url: https://arstechnica.com/tech-policy/2026/02/wikipedia-bans-archive-today-after-site-executed-ddos-and-altered-web-captures/
points: 616
---

Title: Wikipedia deprecates Archive.today, starts removing archive links
Points: 616, Author: nobody9999, Comments: 368

There are many coding agents, but this one is mine.
npm install -g @mariozechner/pi-coding-agent
Pi is a minimal terminal coding harness. Adapt pi to your workflows, not the other way around. Extend it with TypeScript
Pi ships with powerful defaults but skips features like sub-agents and plan mode. Ask pi to build what you want, or install a package that does it your way.
Four modes: interactive, print/JSON,
15+ providers, hundreds of models
Anthropic, OpenAI, Google, Azure, Bedrock, Mistral, Groq, Cerebras, xAI, Hugging Face, Kimi For Coding, MiniMax, OpenRouter, Ollama, and more. Authenticate via API keys or OAuth.
. Cycle through your favorites with
Add custom providers and models via
Tree-structured, shareable history
Sessions are stored as trees. Use
to navigate to any previous point and continue from there. All branches live in a single file. Filter by message type, label entries as bookmarks.
, or upload to a GitHub gist with
and get a shareable URL that renders it.
and extensibility let you do actual context engineering. Control what goes into the context window and how it's managed.
Project instructions loaded at startup from
, parent directories, and the current directory.
Replace or append to the default system prompt per-project.
Auto-summarizes older messages when approaching the context limit. Fully customizable via
: implement topic-based compaction, code-aware summaries, or use different summarization models.
Capability packages with instructions and tools, loaded on-demand. Progressive disclosure without busting the prompt cache. See
Reusable prompts as Markdown files. Type
can inject messages before each turn, filter the message history, implement RAG, or build long-term memory.
Submit messages while the agent works.
sends a steering message (delivered after current tool, interrupts remaining tools).
sends a follow-up (waits until the agent finishes).
Features that other agents bake in, you can build yourself. Extensions are TypeScript modules with access to tools, commands, keyboard shortcuts, events, and the full TUI.
, MCP integration, custom editors, status bars, overlays.
Don't want to build it? Ask pi to build it for you. Or install a
Bundle extensions, skills, prompts, and themes as packages. Install from npm or git:
pi install git:github.com/badlogic/pi-doom
JSON protocol over stdin/stdout for non-Node integrations. See
Pi is aggressively extensible so it doesn't have to dictate your workflow. Features that other tools bake in can be built with
, or installed from third-party
. This keeps the core minimal while letting you shape pi to fit how you work.
Build CLI tools with READMEs (see
), or build an extension that adds MCP support.
There's many ways to do this. Spawn pi instances via tmux, or build your own with
, or install a package that does it your way.
Run in a container, or build your own confirmation flow with
inline with your environment and security requirements.
Write plans to files, or build it with
Use a TOD
